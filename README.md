# miniature-spoon
fancy_Command
###Command Description
This tool looks at the current directory and lists the files for the requested file type
This command can be run in your command line terminal
It combines ls for list files and grep to search for the requested file type
###AI Assistance
I asked AI to explain what options the ls command takes. I also asked AI to explain the grep
  command and its options. I also wasn't sure how to upload the fancy command to github but AI suggested
  I add it to a readme.md file. I'm not sure that this is the correct format so that is where AI may have gotten
  it wrong. 


```bash
ls | grep "\.js$"
